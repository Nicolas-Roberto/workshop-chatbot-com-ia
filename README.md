# workshop-chatbot-com-ia

## Preparando ambiente
#### Dentro do projeto

##### Crie um ambiente virtual
`python3 -m venv .chatbot`

##### Ative o ambiente virtual
`source .chatbot/bin/activate`

##### Instale as dependências
`pip install -r requirements.txt `
